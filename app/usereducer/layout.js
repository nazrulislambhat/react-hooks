export const metadata = {
  title: 'useReducer Hook',
  description: 'useReducer Hook',
};

export default function UseReducerLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-[#F7A072]">{children}</body>
    </html>
  );
}
