export const metadata = {
  title: 'useContext Hook',
  description: 'useContext Hook',
};

export default function UseContextLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-[#F5CDA7]">{children}</body>
    </html>
  );
}
