export const metadata = {
  title: 'useLayoutEffect Hook',
  description: 'useLayoutEffect Hook',
};

export default function UseLayoutEffectLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-[#A4B0F5]">{children}</body>
    </html>
  );
}
