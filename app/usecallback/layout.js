export const metadata = {
  title: 'useCallback Hook',
  description: 'useCallback Hook',
};

export default function UseCallbackLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-[#75DBCD]">{children}</body>
    </html>
  );
}
