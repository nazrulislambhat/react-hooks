export const metadata = {
  title: 'useRef Hook',
  description: 'useRef Hook',
};

export default function UseRefLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-[#DCDBA8]">{children}</body>
    </html>
  );
}
