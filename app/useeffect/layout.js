export const metadata = {
  title: 'useEffect Hook',
  description: 'useEffect Hook',
};

export default function UseEffectLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-[#C9DBBA]">{children}</body>
    </html>
  );
}
