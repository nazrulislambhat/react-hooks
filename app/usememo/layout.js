export const metadata = {
  title: 'useMemo Hook',
  description: 'useMemo Hook',
};

export default function UseMemoLayout({ children }) {
  return (
    <html lang="en">
      <body className="bg-[#FFFD98]">{children}</body>
    </html>
  );
}
